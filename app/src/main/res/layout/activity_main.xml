<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.android.mta16435.p4application.Main">

    <Space
        android:layout_width="150dp"
        android:layout_height="100dp"
        android:layout_alignParentTop="true"
        android:id="@+id/TopMidSpace"
        android:layout_centerHorizontal="true"/>

    <ImageButton
        android:layout_width="@dimen/button_width"
        android:layout_height="@dimen/button_height"
        android:id="@+id/TopLeftButton"
        android:layout_below="@id/TopMidSpace"
        android:layout_toStartOf="@id/TopMidSpace"
        android:contentDescription="Top left button"
        android:scaleType="fitXY"
        android:src="@drawable/the_dark_knight_returns_cover1"
        android:background="#00ffffff"
        android:onClick="goToBatman"/>

    <ImageButton
        android:layout_width="@dimen/button_width"
        android:layout_height="@dimen/button_height"
        android:id="@+id/TopRightButton"
        android:layout_below="@id/TopMidSpace"
        android:layout_toEndOf="@id/TopMidSpace"
        android:contentDescription="Top right button"/>
</RelativeLayout>
